## 👾 оптимизированый код (динамическое программирование)
```c 
#include <stdio.h>
#include <math.h>

int main() {
    int n;

    double P = 1.0;
    int ops = 0;

    double cache = 0;


    printf("Welch Zahl n: ");
    scanf_s("%d", &n);

    for (int i = 1; i <= n; i++) {
        double sumJ = 0;

        double SIN = sin(i);
        double plus = i + SIN;
        ops += 2;

        cache += plus;
        ops ++;

        double k = i * i;
        double zahl = k + 1;
        ops += 2;

        double term = cache / zahl;
        ops ++;

        P *= term;
        ops ++;
    }

    printf("P = %.7f\n", P);
    printf("ops: %d\n", ops);

    return 0;
}
```
